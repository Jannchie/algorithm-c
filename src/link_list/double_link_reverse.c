#include "link_list.h"

struct DoubleListNode
{
    int val;
    struct DoubleListNode *prev;
    struct DoubleListNode *next;
};
struct ListNode *reverseList_iteratively(struct ListNode *head)
{
}
struct ListNode *reverse(List *L, int key)
{
}